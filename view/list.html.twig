{% extends "main.html.twig" %}

{% block css %}
{{ parent() }}
<link rel="stylesheet" type="text/css" href="./resource/css/gridtable.css" />
{% endblock %}

{% block body %}
<table class="gridtable">
	<tr>
		<th>Gracz</th>
		<th>Imię postaci</th>
		<th>Archetyp</th>
		<th>Exp</th>
		<th colspan="2"></th>
	</tr>
{% for sheet in sheets %}
	<tr>
		<td>{{ sheet.getUser().getName() }}</td>
		<td>{{ sheet.getName() }}</td>
		<td>{{ sheet.getArchetype() }}</td>
		<td>{{ sheet.getExp() }}</td>
		<td><a href="sheet/form/{{ sheet.getId() }}"><button>Edytuj</button></a></td>
		<td><a href="sheet/remove/{{ sheet.getId() }}"><button>Usuń</button></a></td>
	</tr>
{% endfor %}
</table>
{% endblock %}